import React, { Component } from 'react';
import './_loginButton.scss';

class LoginButton extends Component {
  render() {
    return (
      <div className="loginButton">
        <button>Login</button>
      </div>
    );
  }
}

export default LoginButton;
